name: Build and Deploy to EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build API image
        run: docker build -t my-devops-api:latest .

      - name: Build client image
        run: docker build -t my-devops-client:latest ./client

      - name: Save images
        run: |
          docker save my-devops-api:latest | gzip > api.tar.gz
          docker save my-devops-client:latest | gzip > client.tar.gz

      - name: Copy to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "api.tar.gz,client.tar.gz,nginx.conf"
          target: ~/

      - name: Deploy on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Load images
            docker load < ~/api.tar.gz
            docker load < ~/client.tar.gz

            # Stop and remove old containers
            docker stop my-devops-api my-devops-client || true
            docker rm my-devops-api my-devops-client || true

            # Ensure data directory exists
            mkdir -p ~/data

            # Start API
            docker run -d \
              --name my-devops-api \
              --restart unless-stopped \
              -p 3000:3000 \
              -v ~/data:/app/data \
              -e ADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }} \
              my-devops-api:latest

            # Start client
            docker run -d \
              --name my-devops-client \
              --restart unless-stopped \
              -p 3001:3000 \
              my-devops-client:latest

            # Update nginx and reload
            sudo cp ~/nginx.conf /etc/nginx/sites-available/default
            sudo nginx -t && sudo systemctl reload nginx

            # Verify both containers are running
            docker ps | grep my-devops
